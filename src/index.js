import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App.js';

const rootElement = document.getElementById('CommitTableList');
ReactDOM.render(
  <App/>,
  rootElement
);
